<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HydraulicJournalApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:HydraulicJournalApp"
    Title="HydraulicJournalApp">

    <TabBar>
        <ShellContent
            Title="Журнал"
            ContentTemplate="{DataTemplate local:MainPage}" />

        <ShellContent
            Title="Разработчики"
            ContentTemplate="{DataTemplate local:DevelopersPage}" />

        <ShellContent
            Title="Клиенты"
            ContentTemplate="{DataTemplate local:CustomersPage}" />

        <ShellContent
            Title="Новая запись"
            ContentTemplate="{DataTemplate local:JournalEntryPage}" />
    </TabBar>

</Shell>